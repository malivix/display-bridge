# Contributing

Read `AGENTS.md`, `docs/development.md`, and `SECURITY.md`. Describe the observable bug or
behavior, keep changes focused, and use Conventional Commits. Include isolated tests and
report physical tests separately. Run `./scripts/verify` before submitting; native changes
also require `./scripts/verify --native`. Share synthetic fixtures, never private diagnostics.
