# Refresh user-facing change summaries

The unreleased changelog now covers current controls/readability features, recovery and
deployment improvements, and their validation limits. It explicitly separates source
updates from installed upgrades and hardware-qualified releases. README links that summary.
The documentation index now points to the latest priority review and source comparison
instead of superseded reports.

Checked every local link target in the three edited entry-point documents and reviewed
the claims against current code and work logs. This is a documentation-only change;
no tests or hardware checks were rerun. Publication privacy checks apply before commit.
