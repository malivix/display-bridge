# UI research and implementation plan refresh

Reviewed the current native menu result/refresh paths and inspected the rebuilt synthetic
Largest brightness dialog. Refreshed BetterDisplay, MonitorControl, Lunar and BenQ Display
Pilot 2 comparisons against official sources. No competitor was installed, no hardware
controls were exercised and no monitor settings changed.

The new dated plan separates existing source features from proposals and physical gaps.
A concrete result-presentation defect takes priority: missing or unexpected monitor/feature
fields can produce plausible default labels, while missing numeric settings appear as zero.
The next implementation slice will validate those responses without weakening controller
ownership checks. Accessibility qualification, longer bounded previews and independent
host setup follow. Documentation entry points now identify one current delivery order.

Validation: reviewed source references and local documentation links; staged privacy and
secret scanning passed. No additional runtime behavior changed in this documentation slice.
Native refactor validation is recorded separately in menu-source-boundaries.md.
