# Native enrollment review entry point

Expose the existing capture-review boundary under Details as Review enrollment. Ask for
Mac A/B explicitly with no preselected role; enable review only after selection. Explain
requirements and that no enrollment is saved. Use the existing command runner and capability
probe so older controllers reject unsupported review without dispatch. Status/recovery stay
available. Allow this inspection when saved controls are unreadable.

Validate the result against the requested host and expected two roles/input map/dimensions.
Render the prospective sizes and discovered audio categories without IDs. Return to a retained
report with installer and physical qualification next steps. No automatic capture/activation.
Demo fixtures block hardware. Verify host choice, mismatch rejection, old-controller behavior,
keyboard/Escape and Largest layout. Mutable guided setup remains subsequent work.
