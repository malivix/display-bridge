# Recovery-first interface

Keep a stable Overview order: summary, recovery, monitors, audio. Put the existing guarded
recovery action before its potentially long explanation. Do not reorder sections or move
focus on each heartbeat. Place current audio output, repair/availability and manual controls
before per-profile preferences. No command, journal, ownership or retry-policy changes.

Verify Largest/minimum-window recovery visually and keyboard traversal in the isolated demo;
retain native stale-action, manual-preservation and preview-token checks. Finish the existing
read-only enrollment-review UI in the same source batch; it remains separate from activation.
Run Python/native checks and exact staged privacy review. Rollback uses the coordinated release;
this work does not install or change hardware.
